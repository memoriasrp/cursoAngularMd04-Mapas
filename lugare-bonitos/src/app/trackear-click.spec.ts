import { TrackearClick } from './trackear-click';

describe('TrackearClick', () => {
  it('should create an instance', () => {
    const directive = new TrackearClick();
    expect(directive).toBeTruthy();
  });
});
